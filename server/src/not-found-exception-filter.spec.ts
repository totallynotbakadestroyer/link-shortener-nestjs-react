import { NotFoundExceptionFilterFilter } from './not-found-exception-filter';

describe('NotFoundExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new NotFoundExceptionFilterFilter()).toBeDefined();
  });
});
